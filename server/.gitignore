node_modules
# Keep environment variables out of version control
.env
.env.staging
.env.production
.DEPLOYMENT.md
.env.docker
# .env.example is intentionally committed (no secrets, just template)
/src/generated/prisma
