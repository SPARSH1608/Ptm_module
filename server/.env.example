# Database
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# App
PORT=3000
JWT_SECRET="generate-with: node -e \"console.log(require('crypto').randomBytes(64).toString('hex'))\""

# External APIs
LAMBDA_URL="https://api-v2-6-0.eapp.vidyamandir.com/mysa/resources"
OTP_LAMBDA_URL="https://api-app.dev.mentomind.com/auth/mysa/teacherOtp"

# Google Meet OAuth
GOOGLE_CLIENT_ID="from Google Cloud Console → APIs & Services → Credentials"
GOOGLE_CLIENT_SECRET="from Google Cloud Console → APIs & Services → Credentials"
GOOGLE_REDIRECT_URI="http://YOUR_DOMAIN/google-callback"
FRONTEND_URL="http://YOUR_DOMAIN"

# Zoom OAuth
ZOOM_CLIENT_ID="from Zoom Marketplace → your app → App Credentials"
ZOOM_CLIENT_SECRET="from Zoom Marketplace → your app → App Credentials"
ZOOM_REDIRECT_URI="http://YOUR_DOMAIN:PORT/api/teacher/zoom-callback"
ZOOM_WEBHOOK_SECRET_TOKEN="from Zoom Marketplace → your app → Event Subscriptions"
